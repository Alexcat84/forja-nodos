# APERTURA CIEGA DE LA VUELTA 30, lote 4 (`scott_radical_candor`): `cap_07` cerrado en insercion y la cabeza `THINK TIME` de `cap_11`

> **SEGUNDA SALIDA DE ESTA MISMA FASE, y lo digo en el primer renglon y no escondido en un anexo.**
> La primera corrio hoy de `14:42` a `15:28` y no llego a sellarse. Lo que la paro no fue esta
> pagina: fue una lista de `config/` a la que le faltaba un nombre. La seccion `0.2` lo cuenta
> entero, con lo que he hecho con lo que aquella corrida dejo escrito.

---

## 0. LO QUE `D.40` ME MANDA TRAER, Y EL ARBOL EN QUE ME ENCUENTRO

### 0.1. LA DECLARACION DE HERENCIA

    ACTA ANTERIOR LEIDA: 148bbd78baae05817d71e200a32bb755a85ca152
    HEREDADO 1: CUMPLIDO
    HEREDADO 2: CUMPLIDO

**Y NO LAS DEJO DESNUDAS, porque una declaracion sin medida es exactamente lo que `D.40` vino a
cerrar.** Las dos van con el comando que las sostiene.

#### `HEREDADO 1`: CUMPLIDO. *La relectura destapa UNA RAZON POR VEZ, y DESPUES de imprimir sus pasos*

**En esta fase he destapado CERO razones**, que es el caso limite del remedio y el unico que se
puede probar sin discutir. **No lo afirmo: lo mido contra todo lo que he escrito hoy.**

    $ (razones de bitacora/VEREDICTOS.jsonl de 40 caracteres o mas, buscadas por su primer trozo
       en TODO lo que he escrito en esta fase)
      razones en la bitacora            : 375
      ficheros mios de esta fase        : 30
      razones destapadas por mi         : 0

**LECTURA:** las cuatro veces que esta fase ha leido la bitacora lo ha hecho **por campos, y
siempre con `razon` fuera**: la cuenta de la seccion `7.6`, las seis lineas `CONTINUA` de la
seccion `9`, el cruce de pares de la `5.1` y el bloque de vigencia. **Ninguna saco una razon, ni
de una en una ni de golpe.** El remedio se estrena de verdad en mi turno normal, cuando releo los
discutibles; **aqui lo que puedo probar es que no me lo he saltado por delante.**

#### `HEREDADO 2`: CUMPLIDO. *El remedio no se rompio, y lo que fallo fue otra cosa*

El `HEREDADO 2` es la comprobacion con `git` de que el `HEREDADO 1` aguanto, **y su sustancia
propia es la otra mitad**: la caida de la vuelta 28 no fue un rechazo mal registrado, fue **una
corrida mia paralela que deshizo lo que otra corrida mia acababa de escribir**. Lo que eso me
manda en esta fase es no repetirlo.

    $ git status --porcelain dataset/ bitacora/ censos/ config/ cuarentena/ esquema/
      (sin una sola linea)

**LECTURA:** **ninguna sede de dato se ha movido en esta fase.** Y lo digo entero, porque hay algo
que declarar: **he corrido dos cosas a la vez**, el barrido de vecinos en segundo plano mientras
las pruebas de aceptacion corrian delante. **Son dos corridas mias en paralelo, que es la figura
del `HEREDADO 2`.** La diferencia esta medida arriba y no argumentada: **el informe de la aduana
es de cero inserciones por construccion y mis poblaciones son copias**, asi que ninguna de las dos
escribe donde la de la vuelta 28 escribio. **La figura estaba, el dano no.**

### 0.2. **ESTA ES LA SEGUNDA SALIDA, Y QUE HE HECHO CON LA PRIMERA**

**Que paso, leido del propio arbol y no de mi memoria:**

    $ git log --format="%h %ad %s" --date=format:"%H:%M:%S" -1
      96274b3 15:36:26 ARNES: me deje el cuarto fichero, y el bucle se paro por una caida que no existia

La primera salida de esta fase **cerro su trabajo y no pudo sellarlo**: el censo de rutas estaba en
rojo porque una acta mia cita `docs/loop/loop.log` como sede, `D.34.2` lo retira durante esta fase,
y la lista de sedes vacias de `config/` **traia tres de los cuatro retirados y no el cuarto**. El
dictamen esta archivado en `docs/loop/paradas/2026-09-16-el-cuarto-fichero.md` y se declara como
especie `ARNES`: *no es una caida del auditor ni del extractor, y sus rachas no se mueven.*

**LO QUE SOBREVIVIO, y es mio:** los ficheros de trabajo de aquella corrida, en `.v30a/`. El
fichero `docs/loop/APERTURA_CIEGA.md` no: el arnes lo borra al reinvocarme.

| | |
|---|---|
| **que he hecho con `.v30a/`** | **lo he leido.** Es obra mia, de esta misma fase y de esta misma vuelta, escrita con los cuatro ficheros de `D.34.2` fuera del arbol. No es ninguno de los cuatro, y no lo he recuperado de `git`: estaba en el arbol |
| **que NO he hecho** | **copiar ni una cifra.** Los trece barridos, las guardas, el bloque de vigencia, las pruebas, el cruce de pares y la cobertura del capitulo **los he vuelto a correr en esta fase**, y lo que publico sale de esa corrida |
| **donde esta lo de hoy** | en `.v30b/`, y cada cifra de esta pagina lleva pegada la salida de ahi |

> **Y PUBLICO LAS DOS VECES QUE LO DE HOY NO COINCIDE CON LO QUE AQUELLA PAGINA PUBLICO**, porque
> esa es la unica parte que importa de esta declaracion:
>
> | donde | la primera salida dijo | mi instrumento de hoy dice | que ha pasado |
> |---|---|---|---|
> | lineas de `cap_07.md` | `434` | **`433`** | `wc -l` da 433. La cifra vieja estaba una linea alta |
> | el cruce de pares | `82` contra `86` | **`65` contra `69`** | son **unidades distintas** y las dos son ciertas: 82 y 86 son vecindades levantadas y lineas escritas; 65 y 69 son **pares distintos sin orden**. La `5.1` publica las dos, dichas por su nombre |
>
> **Ninguna de las dos mueve una clase.** Las digo porque una pagina que se rehace y no dice en que
> se corrige a si misma **esta pidiendo que la crean, no que la comprueben.**

### 0.3. LO QUE LA VUELTA MOVIO, MEDIDO ANTES DE LEER NADA

    $ git log --diff-filter=R --find-renames=100% --name-status --format='' 8e626b6..HEAD -- cuarentena | grep -c '^R100'
      13
    $ wc -l bitacora/VEREDICTOS.jsonl dataset/nodos.jsonl
        375 bitacora/VEREDICTOS.jsonl
        256 dataset/nodos.jsonl
    $ git show b9f484a:bitacora/VEREDICTOS.jsonl | wc -l
      289
    $ python forja.py gate
      GATE VERDE.
        nodos verificados: 256

**LECTURA:** **trece ficheros archivados en `cuarentena/_insertados/` y trece nodos mas en el
grafo**, porque el testigo que el arnes escribio antes de abrir la vuelta daba `243`. **Trece por
tres caminos que no se copian entre si:** el conteo de renombrados de `git`, la diferencia de las
dos cuentas del gate, y los trece candidatos distintos que firman las lineas nuevas de la bitacora
(seccion `7.6`).
