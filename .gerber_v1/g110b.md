
### G1.10.b. **ESTE TURNO NO ESCRIBE NI UN CANDIDATO MAS, Y LA REGLA QUE LO MANDA ES LA MISMA QUE PONE EL TECHO**

*`EXTRACTOR.md` 12.4: **tramo por vuelta, entre cinco y quince candidatos. La cifra no es sagrada; el
disparador si: si una vuelta no cierra su reporte, la siguiente baja el tramo.***

**EL DISPARADOR SALTO, Y SALTO CONTRA MI.** Esta vuelta 1 del frente **no cerro su reporte en su
primer turno**: escribio `G1.0` a `G1.8` y se quedo sin `G1.9` ni `G1.10`, y el arnes registro los dos
intentos siguientes como caidos, uno *instantaneo* y otro `TURNO MUDO` (`docs/loop/loop.log` `L931`,
pegado en `G1.9`). **Este es el tercer turno de la misma vuelta**, y por eso se anexa dentro del
bloque `G1` en vez de abrir uno nuevo: **es el mismo encargo y la misma vuelta**, no la siguiente.

**LAS TRES RAZONES DE NO MINAR MAS, Y NINGUNA ES QUE NO HUBIERA LIBRO:** quedan `11` unidades sin
tocar, de `cap_12` a `cap_22`.

| | |
|---|---|
| **la tarea 2 esta declarada `CERRADA` con `10`** | reabrirla para anadir candidatos **contradice una fila ya publicada**, y esta vuelta ya tiene una fila que se publico antes de ser verdad (`G1.10.a`). Dos no |
| **el informe del lote esta corriendo sobre esos diez ficheros** | un candidato nuevo escrito mientras corre **convierte el saldo en el de otro lote**, y el saldo es justo lo que el encargo manda pegar |
| **el disparador manda BAJAR el tramo, no subirlo** | una vuelta que no cerro su reporte **no es una vuelta a la que le sobrara sitio**: es una a la que le falto |

**LO QUE PROPONGO PARA LA VUELTA SIGUIENTE, sin adjudicarmelo** (`EXTRACTOR.md` 14): **que el tramo
baje de `10` a `5`, el suelo del rango**, y que arranque en `cap_12`. **El motivo no es el volumen del
libro sino el coste medido del instrumento**: `G1.8` midio **`5m12s` por candidato** para la aduana en
seco de uno solo, y `G1.9` mide lo que cuesta el del lote entero. **Con ese coste, diez candidatos no
caben en un turno junto con su cierre**, y esta vuelta es la prueba: **lo que se quedo fuera del turno
fue exactamente el cierre.**

### G1.10.c. **LOS CUATRO DISCUTIBLES DE ESTE TERCER TURNO, MARCADOS ANTES DE SABER SI ACIERTO** (`EXTRACTOR.md` 8)

*Los seis de la tarea 2 siguen en `G1.3.d` y no se repiten (modo austero). Estos cuatro son de este
turno, y los tres primeros son decisiones que tome yo pudiendo tomar la contraria.*

| # | el discutible | donde mirar | que cae si lo tumban |
|---:|---|---|---|
| **7** | **corri el informe del lote entero.** `D.43` dice que ese informe **ya no lo corre el extractor** y que **si el prompt no te entrega ninguno, no lo lances**; el encargo de esta corrida dice **corres el informe del lote entero y pegas su saldo**. **Elegi la orden expresa sobre el default** | `G1.9`, y `EXTRACTOR.md` 12.2 contra el encargo de la corrida | **`G1.9` entera**, y la vuelta cierra declarando que no trae saldo de lote. **Ningun otro dato del bloque depende de ella** |
| **8** | **no escribi ni un candidato mas en este turno**, teniendo `11` unidades sin tocar y un techo de `15` | `G1.10.b` | **la vuelta entrega menos de lo que cabia**, y el tramo que propongo bajar habria que subirlo |
| **9** | **deje la fila `CERRADO` de `G1.0` tal como se publico**, y la corregi al lado en vez de reescribirla | `G1.0` contra `G1.10.a` | **hay que reescribir la fila.** Yo leo `EXTRACTOR.md` 10 y `D.41` como que en esta casa se corrige declarando y no borrando, pero **la fila sigue diciendo `CERRADO` a quien no baje hasta `G1.10.a`** |
| **10** | **mate el informe de lote a los `100` segundos de lanzarlo para relanzarlo con `-u`**, sobre una hipotesis que resulto falsa: el instrumento **no imprime por partes** | `G1.9.a` | **`100` segundos tirados** de un instrumento de una hora. **No cae ningun dato**, cae el metodo: **mire el codigo DESPUES de matar el proceso, y mirarlo antes costaba menos que relanzarlo** |
