# DECISION DEL FUNDADOR, 24 SEP 2026: **CIERRE DE LA CAMPANIA DE NODOS**

*La decision va arriba, literal. Debajo, donde queda aplicada.*

---

## LA DECISION, LITERAL

> SESION DE CHAT en forja-nodos. Vigila solo leyendo si alguna linea esta
> a mitad de turno. DECISION DEL FUNDADOR (24 sep 2026), CIERRE DE LA
> CAMPAÑA DE NODOS, escribela en el TABLERO y en PARALELO.md:
>
> 1. EL MUNDO 10 NO ENTRA: se ANULA la extraccion de
>    gerber_emyth_cap17_reservado. El capitulo 17 queda sin extraer y los
>    6 nodos de ONU se quedan en el grafo de la forja, fuera de todo pack,
>    como semilla guardada. Nada se borra.
> 2. EL MUNDO SIGUE SIENDO EL 11 (Primer Equipo): no se renumera nada. El
>    tag de cierre se llama primer-equipo-completo.
> 3. EL FRENTE DE EXTRACCION SE CIERRA PARA SIEMPRE al cosechar marquet.
>    La serial inserta grove, gerber y marquet; con gate, guiones, suite y
>    cierre en verde, crea y empuja el tag primer-equipo-completo, y tu
>    reporte abre con PRIMER EQUIPO COMPLETO, el hash y el censo por
>    libro. Despues la serial SE DETIENE: la campaña de nodos queda
>    CERRADA, con su acta final en docs/loop/.

---

## DONDE QUEDA APLICADA

| punto | donde |
|---|---|
| `1` el mundo `10` no entra | `config/frentes.json`, clave `anulados`, con su cita. **El tablero publica `gerber_emyth_cap17_reservado` como `ANULADO`**, no como `SIN EMPEZAR`. Nada se borra: sus fuentes siguen en `fuentes/`, y los `6` nodos de `onu_consumidor` siguen en el grafo |
| `2` el mundo es el `11` | no se renumera nada. El tag de cierre es **`primer-equipo-completo`** |
| `3` el cierre | `PARALELO.md` seccion `8`: el frente se cierra para siempre al cosechar Marquet; la serial inserta Grove, Gerber y Marquet; **con gate, guiones, suite y cierre en verde se crea y se empuja el tag**; el reporte abre con `PRIMER EQUIPO COMPLETO`, el hash y el censo por libro; y **la serial se detiene** con su acta final en `docs/loop/` |

> **LO QUE ESTA DECISION FIJA SIN DECIRLO, y conviene tenerlo delante:** despues de Marquet no
> hay siguiente libro. **`D.32` ya no abre lote**, y el encargo de la ultima tanda de insercion
> tiene que decirlo: al cerrar, `PARA_ALEXIS` de cierre y `PROMPT_SIGUIENTE` vacio.
