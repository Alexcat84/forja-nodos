# ENCARGO DE LA VUELTA 22: **LA PIEZA QUE `cap_10` DEBE**, `cap_11`, Y LA CIFRA DE FIDELIDAD QUE EL INSTRUMENTO ANCHO DEJA SIN MEDIR

*Escrito por el **auditor** al cerrar la **ACTA 21** (`docs/loop/ACTA_AUDITOR.md`, la seccion que
abre con `# ACTA 21`). Sede del auditor por `AUDITOR_FORJA.md` 5.6.*

**Commitea y pushea lo pendiente en la rama activa antes de tocar nada.**

---

## LO QUE LA `ACTA 21` DEJA DECIDIDO, EN SEIS LINEAS

- **TU VUELTA 21 SALE LIMPIA DE `CLASE` Y DE `CIFRA PUBLICADA`, por tercera vez seguida.** `cap_10`
  cerrado en **13 de 13** con la frontera al digito, **194 pasos**, **9 filas de vecino** que mi
  propio barrido sobre los `299` reprodujo **una a una**, y tus **siete lecturas `SANO` releidas
  todas: las siete se sostienen.**
- **DE TUS NUEVE DISCUTIBLES, SIETE SE SOSTIENEN ENTEROS, UNO A MEDIAS Y UNO CAE. Y EL QUE CAE ES
  MIO:** tu discutible 1 tenia razon. **La frontera de `cap_10` es `14` y no `13`**, y la cifra
  falsa estaba en mis dos sedes. **No te cuesta nada a ti.**
- **TU RACHA `REPORTE` SE QUEDA EN `2 de 3` Y NO SUBE.** Dos caidas registradas y **ninguna
  acumula**: las dos filas de ruta auto referenciales, y la razon del par de la lupa.
- **EL CRITERIO DE `PASOS INVENTADOS` ESTABA MAL Y ERA MIO** (`ACTA 20` `6.2`). Corregido contra
  `D.30`: **el numerador cuenta los puentes escritos y cazados, se hayan corregido o no.** Tu
  `cap_10` queda en **`17 de 194 = 8,76`**, y **el freno NO se dispara**.
- **TUS CUATRO PROPUESTAS: LAS CUATRO ADJUDICADAS.** La 1 y la 2 van encargadas aqui; la 3 la traigo
  yo a Alexis medida (`ACTA 21` `11.1`) y **no la tocas tu**; la 4 sigue abierta y visible.
- **LAS TRES ARISTAS `D.37` NO CABLEADAS NO SON CAIDA TUYA: LA NEGRITA DE MI ENCARGO ESTABA MAL
  CITADA.** `D.37` dice **`INSERTAS`**, no *escribes*. Corregido abajo.

---

## TAREA 1: LOS REGISTROS

**1.a. LEE LA `ACTA 21` ENTERA** (`docs/loop/ACTA_AUDITOR.md`, desde `# ACTA 21`) y recoge sus
adjudicaciones **sin reabrirlas**, salvo que encuentres un hecho nuevo; si lo encuentras, **lo traes
con su medida y no lo resuelves copiando** (`EXTRACTOR.md` 5).

**1.b. LAS OCHO ADJUDICACIONES, para que no las midas contra su version vieja:**

| # | adjudicacion | donde |
|---:|---|---|
| **1** | **la frontera de `cap_10` es `14`**: falta la cabeza de las conversaciones de carrera | `4.1` |
| **2** | los cinco cortes que sostuviste **quedan confirmados** habiendo leido tus pasos | `4.2` |
| **3** | las tres aristas `D.37` **no cableadas NO son parada**, y el defecto es de mi encargo | `4.3` |
| **4** | `facilitar_despido_tres_cosas` **es nodo**, y su `entregable_esperado` **se corrige** | `4.4` |
| **5** | los hermanos de serie son **`SANO`**, por extension natural de `EXTRACTOR.md` 15.6 | `4.5` |
| **6** | el par de la lupa es **`CONTINUA` con arista** y **su razon escrita se corrige** | `4.6` |
| **7** | las dos filas de ruta auto referenciales: `REPORTE` registrada que **no acumula** | `4.7` |
| **8** | **el criterio de `PASOS INVENTADOS` cuenta los puentes escritos y cazados**, corregidos o no | `4.8` |

**1.c. Y NO TOQUES `docs/BANCO_DE_REGLAS.md`, `config/` ni `AUDITOR_FORJA.md`.** La discrepancia de
`D.38.5` (*ya lo levanta* contra la medida que dice que no) **va a Alexis por mi acta `11.1`**, y
**no es tuya de arreglar**. Tu PROPUESTA 3 estaba bien planteada y por eso la recojo yo.

---

## TAREA 2, **BLOQUEANTE Y PRIMERA**: LA PIEZA QUE `cap_10` DEBE, Y LAS DOS CORRECCIONES DE FICHERO

### 2.a. **LA PIEZA 14: LA CABEZA DE LA SERIE DE LAS CONVERSACIONES DE CARRERA**

**LA REGLA QUE LA MANDA, y la cito como esta:** manual 3.4, *si es serie numerada de un libro: un
nodo por paso **mas UNA cabeza**, jamas dos compresiones de la misma numeracion.* **La cabeza no
esta prohibida: esta prescrita.** Lo prohibido es la **segunda**.

**EL MATERIAL, Y SON TRES LINEAS, NO UNA:** `L19`, `L21` y `L43`, **254 palabras** medidas con
`sed -n '19p;21p;43p' | wc -w`. **Remidelo tu.**

| | |
|---|---|
| **condicion de activacion** | vas a desplegar las conversaciones de carrera con tu equipo, o vas a ensenar a tus jefes a tenerlas |
| **entregable esperado** | las tres conversaciones tenidas con cada persona a tu cargo, en el curso de tres a seis semanas y dentro de los 1:1 de siempre |
| **los cinco actos que hoy no viajan a ningun nodo** | tenerlas con **cada** persona a tu cargo sea cual sea su trayectoria (`L19`); que **caben en el hueco de 1:1 de siempre** (`L21`); que son **lo primero** que se hace al desplegar franqueza radical (`L21`); **tres de cuarenta y cinco minutos en el curso de tres a seis semanas** (`L43`); **ensenar a los jefes de tu equipo a tenerlas** (`L43`) |

**LO QUE `L43` ES Y NO ES:** esta dentro del relato de Russ, **y manual 3.5 dice exactamente que
hacer con eso**: *el caso no es la casa. La doctrina vive en su nodo y el caso entra como ejemplo
nombrado dentro de ella.* **La cadencia es doctrina y viaja; el nombre de Russ, Google y la encuesta
se quedan en el caso.** La señal barata: **el entregable no puede llevar ni un dato del caso.**

**Y COMPROBADO QUE NO VIAJA, con la busqueda corrida y no supuesta:**

    $ grep -l -i "seis semanas" cuarentena/scott_radical_candor/*.json
      (cero lineas)

**LOS 45 MINUTOS QUE SI VIAJAN SON OTROS:** el paso 15 de `conversar_historia_vida` transcribe
`L59`, que habla **de la primera conversacion sola**. **No lo toques.**

### 2.b. **LAS TRES ARISTAS `D.37` DE ESTA CABEZA, DECLARADAS ENTERAS**

**La cuenta esta escrita dos veces** (`L43` y `L91`, *three conversations*) **y las tres partes
existen y estan escritas**: `conversar_historia_vida_descubrir_motivadores`,
`conversar_suenios_cruzar_habilidades`, `trazar_plan_dieciocho_meses_aprendizaje`.

> ### **Y AQUI VA LA CORRECCION DE MI PROPIO ENCARGO ANTERIOR, PARA QUE NO VUELVA A PEDIRTE UNA OPERACION QUE NO EXISTE:**
>
> **`D.37` y `EXTRACTOR.md` 15.6 dicen `DECLARAS ESAS ARISTAS EN LA MISMA VUELTA EN QUE **INSERTAS**
> LAS PARTES`.** Mi encargo de la vuelta 21 escribio *se escriban* y **eso es lo que estaba mal, no
> tu**. Asi que: **las tres se DECLARAN enteras hoy** (madre, hijo, `--paso` de la madre que las
> enumera, y razon), **y se CABLEAN el dia de la insercion.** **No corras `forja.py arista` contra
> ids que viven en cuarentena: la primera guarda del modulo lo rechaza y lo comprobamos los dos.**

### 2.c. **LAS DOS CORRECCIONES DE FICHERO, declaradas y sin borrar el texto viejo**

| fichero | que se corrige | por que |
|---|---|---|
| `facilitar_despido_tres_cosas.json`, `entregable_esperado` | quita **y tu empresa situada respecto a los dos errores opuestos**; deja las tres cosas identificadas y encargadas como el plan del despido | `L169` y `L171` **describen** dos errores de empresa, **no encargan situar ninguna**. Situar una empresa no deja fichero (`ACTA 21` `4.4`). **La mitad retirada NO es un paso: no toques los 11 pasos y no muevas `PASOS INVENTADOS` por esto** |
| `reconocer_excelencia_trayectoria_gradual.json`, la razon del par en su `resumen_teorico` | la razon correcta es: **la madre nombra las vias Y DESPLIEGA LA DEL EXPERTO DE REFERENCIA EN CUATRO PASOS (7, 8, 9 y 10); la hija anade la via del agradecimiento entera, con la distincion escrita frente al elogio, y el plazo de preparacion de la clase que la madre no tiene; el acto del honor y no la obligacion es COMPARTIDO y no es lo que separa el par** | la razon de hoy dice que la madre *nombra las vias en una linea*, **y no es cierto**: la lei entera (`ACTA 21` `4.6`). **La clase NO cambia: sigue `CONTINUA` con arista, `--paso 2`.** Lo que cambia es la razon **que se va a cablear al grafo el dia de la insercion**, y esa si es duradera |

---

## TAREA 3: **`cap_11` ENTERO**, Y `cap_12` SOLO SI CABE BAJO EL TECHO

| | |
|---|---|
| unidad | `cap_11`, **`Cap. 8`** |
| cuerpo | **8.626 palabras** (`sed -n '8,$p' \| wc -w`, **remidelo tu**) |
| proyeccion | **~13,5** a la densidad medida de `cap_10` (`8.976 / 14 = 641` palabras por pieza) |
| techo | **15** (`EXTRACTOR.md` 12.4) |

**`cap_11` ENTRA ENTERO. `cap_12` (`Getting Started`, 2.118 palabras, proyecta ~3) ENTRA SOLO SI LA
CUENTA REAL DE `cap_11` DEJA HUECO BAJO 15, Y LO DECLARAS CON SU CIFRA EN LOS DOS CASOS.**

> **Y LO QUE TE TOCA DECLARAR SI CIERRAS CORTO:** `12.4` dice que si un solo capitulo pasa del techo,
> **la vuelta cierra en ese capitulo y lo declara**. Una vuelta que cierra corto **y no lo dice** es
> caida de especie `REPORTE`. **La proyeccion por densidad fallo en `cap_10`** (predijo `10,3` y
> fueron `14`): **usala como orden de magnitud, mide la cuenta real y publica las dos.**

**LA FRONTERA LA PUBLICAS TU ANTES DE CORTAR**, con su cierre contra el cuerpo (`ACTA 18` `7.5`
orden 1) y **con la fila de residuo en cero o con sus lineas nombradas UNA A UNA** (`ACTA 19` `7.4`
ORDEN A). **Esa orden es la que cazo la pieza 14 de `cap_10`**, y por eso no se relaja: **una fila
de resto que dice *entrada, rotulos y el caso* en 1.641 palabras esconde doctrina dentro.**

---

## TAREA 4: **LA CIFRA DE FIDELIDAD QUE FALTA, MEDIDA Y ACOTADA** (es tu PROPUESTA 1, y ahora es una deuda de cifra)

**POR QUE SUBE A TAREA:** con el criterio corregido (`ACTA 21` `4.8`), **siete filas de
`PASOS INVENTADOS` estan marcadas `sin medir con el instrumento ancho`**, y el total del lote
(`2,19`) **es un suelo y no una medida**. Tu propia cifra lo sostiene: el estrecho caza **11** de
las 22 y el ancho caza **22**. **Los 856 pasos de `cap_00` a `cap_09` se escribieron con el estrecho
delante.**

**Y VA ACOTADA A PROPOSITO, PORQUE UNA RELECTURA SIN TECHO SE COME EL TRABAJO QUE VIGILA** (cosecha
`7.G`):

1. **CORRE `.t1_v21/cuentas21.py` sobre `cap_00` a `cap_09`** y publica **las ocurrencias por
   capitulo**, con su salida pegada. **Eso es una medida de ocurrencias, no un numerador**, y lo
   dices asi.
2. **LEE UNA A UNA, contra su linea, SOLO DOS CAPITULOS:** el de **mas ocurrencias** y **`cap_04`**,
   que es la fila heredada mas alta. **De esos dos firmas el numerador.**
3. **LOS DEMAS SE QUEDAN EN `sin leer`, con su cuenta de ocurrencias publicada**, y **no los
   corriges esta vuelta**: corregir 856 pasos no cabe y no se empieza lo que no se cierra.
4. **NO ES MAQUINARIA NUEVA** (moratoria, `EXTRACTOR.md` 13): el instrumento **ya existe y lo
   escribiste tu**. Lo que se encarga es **correrlo**, no escribir otro.

---

## TAREA 5: **LAS DOS FILAS DE RUTA QUE NO PUEDEN SER VERDAD, Y LA FILA DE ALCANCE DE `_insertados`**

*Es la escalada encargada y no solo declarada (`AUDITOR_FORJA.md` 1 punto 4): tu racha `REPORTE`
esta en `2 de 3` y **la misma tabla ha fallado dos vueltas seguidas**, aunque ninguna acumule.*

**EL DIAGNOSTICO, MEDIDO POR MI CON LA HORA DE CADA FICHERO** (`ACTA 21` `1.5`): tu remedio de la
vuelta 20 **se ejecuto a la letra** y aun asi dos filas salieron falsas, **porque cuentan el fichero
que el propio acto de publicar crea**. `.t1_v21/*.txt` decia `13` y hoy hay `14`; `.v21/*.md` decia
`14` y hoy hay `16`. **Las dos eran ciertas en el segundo en que se contaron.**

| que haces | |
|---|---|
| **las filas que cuentan artefactos que el acto de publicar crea** (`.vNN/` de fragmentos del reporte, y las salidas que se guardan al cerrar) | **o se cuentan con el fichero excluido Y SE DICE CUAL**, o **no se publican**. Las dos salidas valen; lo que no vale es una cifra que caduca en un minuto |
| **las filas de artefactos cerrados** (`.aduana_vNN/`, `sellos`, los guiones) | **se quedan como estan: las siete reprodujeron al digito** |
| **`cuarentena/_insertados/`** | tu `O.0` imprime `0` y tu `O.10` dice `201`. **Las dos salidas son ciertas** (glob plano contra `find` recursivo) y **ninguna es caida**, pero **una carpeta con dos numeros y sin reconciliar invita a sospechar**: escribe **el alcance del comando en la misma fila** |

---

## SON CINCO TAREAS Y EL TOPE SON CINCO

**LA 2 ES BLOQUEANTE Y VA PRIMERA:** es corta, cierra una deuda que abri yo, y **`cap_10` no esta
cerrado hasta que exista**. Si `cap_11` se come la vuelta, **la vuelta cierra ahi y lo declaras con
su cifra**; lo que puede pasar a la siguiente es la **TAREA 4** y `cap_12`.

## LO QUE NO HACE ESTA VUELTA

**NO INSERTA NADA.** `D.39` inserta un lote **CERRADO** y quedan `cap_12` a `cap_14` sin minar.
**Cero inserciones no es una vuelta perdida.** Y la deuda de aristas sube: **16 pendientes al cerrar
la 21** (12 `D.29`, 3 `D.37`, 1 par de lectura) **mas las 3 `D.37` de la pieza 14 son 19**, y
**dieciocho de las diecinueve se desbloquean con el mismo acto**, el cierre del lote 4. **Repitelas
al cierre enteras, sin resumirlas y sin inventarles sede.**

**Y NO MUEVE EL VOLUMEN.** El tramo sigue en **tres capitulos**: `PASOS INVENTADOS` da peor fila
`cap_10` **8,76 contra tope 10** y **no se dispara**. Esta vuelta corre a uno o dos **porque manda
el techo de candidatos**, no porque el freno se haya movido. **Publica la tabla igual, fila por
capitulo mas total del lote, y con el criterio corregido de `4.8`.**

## LAS PARADAS

**Las de `EXTRACTOR.md` 7 estan enteras.** Y una precision que sale de tu propia PROPUESTA 2: **si
un encargo mio vuelve a contradecir una regla que el mismo cita, eso SI es parada y la traes.** Hoy
no lo fue porque cuatro piezas escritas coincidian y la negrita estaba sola; **no cuentes con que se
repita.**

**Cero guiones largos y cero guiones medios. Deja correr el hook. Si algo contradice una regla
vigente, paras y lo traes. No adivines.**
