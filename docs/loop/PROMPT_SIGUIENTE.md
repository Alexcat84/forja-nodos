# ENCARGO DE LA VUELTA 5 DEL FRENTE `gerber_emyth`: **`cap_15`, `cap_16` Y `cap_17`**, y las dos correcciones declaradas que deja la `ACTA G4`

*Linea **`gerber_emyth`** (`extraccion-gerber_emyth`, worktree
`C:/Users/AlexDesk/Documents/forja-gerber_emyth`). **Escrito por el auditor del bucle** al
cerrar la `ACTA G4`, que audita tu vuelta `4`.*

> # **LIBRO DE ESTA VUELTA: gerber_emyth**
> # **CLASE DE ESTA VUELTA: EXTRACCION**

**Commitea y pushea lo pendiente en la rama activa antes de tocar nada.**

---

## 0. COMO TE FUE, EN UNA TABLA, PARA QUE NO TENGAS QUE LEER EL ACTA ENTERA

| lo que mide la `ACTA G4` | resultado |
|---|---|
| `d101`, la condicion del reinicio de tu racha | **PAGADA ENTERA**, y verificada pieza por pieza (`L51`, `L63`, `L69`) |
| las tres fronteras, recompuestas por el auditor **sin tu instrumento** | **al digito las tres** (`4206`, `364`, `3695`, residuo `0`) |
| tus `25` pasos, leidos **los `25`** contra su parrafo | **`0` PUENTE. Las tres filas de `PASOS INVENTADOS` te quedan FIRMADAS en `0,00` por ciento** |
| tu muestra con semilla `g4` | **identica byte a byte** |
| tus cuatro informes de aduana, vueltos a correr | **tres identicos al milesimo**, el cuarto gana dos vecinos **por el orden, no por discrepancia** |
| tus **tres** discutibles marcados | **se sostienen los tres** |
| cuatro superficies que **no** marcaste y que el auditor examino | **se sostienen las cuatro** |
| tus cinco rachas | **`REPORTE 0 de 3`, `CIFRA PUBLICADA 0 de 2`, `CLASE 0 de 2`, `DATO MOVIDO 0 de 2`**, ninguna en su tope |

**LO QUE SE CAE SON DOS FRASES, NO UNA CIFRA**, y las dos viven en prosa de acompaniamiento, que es
donde `5.2` dice que **NO acumula**. Se corrigen en la `TAREA 1` y ahi se acaba.

> ### **Y UNA CIFRA QUE NO ES TUYA PERO TE TOCA SABERLA: TU TURNO COSTO `16,8261033` USD**
>
> El objetivo del regimen ligero es **`5`** (`D.58`) y el tope de declaracion de `D.55` es **`10`**.
> **El desglose esta medido en la `ACTA G4` `6.4`, y no es lo que escribes: es lo que relees.** El
> `98,69` por ciento de tus tokens fue relectura de contexto, `245 098` por turno durante `166`
> turnos, y la sede que domina esa relectura es `docs/loop/REPORTE.md`, que pesa **`4,09` MB**.
>
> **NO TE ENCARGO QUE LO ARREGLES**, porque rotar esa sede es decision del fundador y esta declarada
> para el en la `ACTA G4` `6.4`. **Lo que si te encargo es `D.47` con esa cifra delante:** tu bloque
> de esta vuelta **no repite nada que el registro ya diga**, las cifras van talladas y sin parrafo de
> acompaniamiento, y los discutibles van **por numero y linea**. **Lo que se quita es la repeticion,
> nunca la prueba.**

---

## 1. TAREA `1`: **LAS DOS CORRECCIONES DECLARADAS, TACHADAS EN SU SITIO Y SIN BORRAR**

**Las dos estan en PROSA, no en tabla tallada**, asi que aqui **si se tacha y se escribe al lado**.
No es el caso de `R3` de la vuelta `4`: alli hiciste bien en no tocar la celda, porque la tabla era
TALLADA y `D.41` manda regenerar, nunca teclear. **Distingue las dos cosas y la casa deja de
tropezar con esto.**

| # | donde | que dice | que tiene que decir |
|---:|---|---|---|
| `1` | `docs/loop/REPORTE.md`, `G4.6.b`, parrafo en cursiva | *`CREDITO_gerber_emyth.jsonl` no tiene ni una fila cuyo `tanda` no sea una `ACTA` del auditor* | **es falso**: la **primera** fila tiene `tanda` `"G2"` y `cita` `REPORTE.md seccion G2`. Es justo la fila que la `ACTA G2` `3.2` cargo como `CIFRA PUBLICADA`. Tachala y escribe al lado la salida de `head -1 docs/loop/CREDITO_gerber_emyth.jsonl` |
| `2` | `docs/loop/REPORTE.md`, `G4.3.a`, parrafo de acompaniamiento | *Mismo patron que `fingir_prototipo_cinco_mil_replicas* (**entro** *con `4` de `6` partes)* | **no ha entrado**: esta en `cuarentena/gerber_emyth/` y hay **`0`** nodos de `gerber_emyth` en el grafo, y su `nodos_siguientes` esta vacio. Lo cierto es que **nacio con `4` de `6` reglas escritas**. Tachalo y escribe la salida del conteo al lado |

> ### **Y LA CONCLUSION DE `G4.6.b` NO SE TOCA, PORQUE ACERTASTE**
>
> **Tenias razon en no escribirte tu propia fila de credito.** No la tenias por el argumento que
> escribiste, la tenias por el precedente: **esa fila existe, y costo una `CIFRA PUBLICADA` a la
> vuelta `2`** (`ACTA G2` `3.2`). **Asi que queda dicho de una vez y no lo vuelvas a razonar cada
> vuelta:** cuando un encargo diga *escribe tu tanda*, **tu la MIDES con `python forja.py credito`,
> que es de solo lectura, y quien la ANOTA con `--anotar` es el auditor, con el nombre de su acta en
> el campo `tanda`.** Este encargo ya no te lo pide.

---

## 2. TAREA `2`: **`cap_15`, `cap_16` Y `cap_17`**

**Tres capitulos, que es el techo del ligero** (`D.58`: tres capitulos, techo `30` candidatos). **Y
el volumen no baja**, porque la fila que lo decide (`AUDITOR_FORJA.md` `8.2`, el PEOR capitulo) te
salio en `0,00` por ciento en los tres.

| fichero | unidad del libro | titulo textual | palabras |
|---|---|---|---:|
| `cap_15.md` | `Cap. 13` | *Your Strategic Objective* | `4685` |
| `cap_16.md` | `Cap. 14` | *Your Organizational Strategy* | `4835` |
| `cap_17.md` | `Cap. 15` | *Your Management Strategy* | `2448` |

> ### **LA TRAMPA DEL NOMBRE, Y LEELA ANTES DE ABRIR NADA**
>
> **`fuentes/gerber_emyth/cap_17.md` NO es el capitulo reservado.** Es `Cap. 15`,
> *Your Management Strategy*. **El reservado es `fuentes/gerber_emyth_cap17_reservado/cap_17.md`**,
> que es `Cap. 17`, *Your Marketing Strategy*, y **ese no se toca**. Los nombres coinciden y la
> numeracion del fichero va dos por delante de la del libro. **Compruebalo con
> `grep -m1 "^unidad:"` antes de cortar, y pega la salida.**

Por cada capitulo, y en este orden:

1. **LA FRONTERA, fila a fila contra el fichero**, con sus numeros de linea y sus palabras, **cero
   solapes y cero lineas sin cubrir**, comprobada al digito. **Tallada** (`D.41`).
2. **Los candidatos que de, cada uno con su `python forja.py informe` corrido en el mismo acto en
   que lo escribes** (`EXTRACTOR.md` 16), y su relectura de fidelidad `D.30` **en el acto**, con la
   cita pegada (`D.35`).
3. **`PASOS INVENTADOS POR CAPITULO`, UNA FILA POR CAPITULO Y NO UNA MEDIA.** **Si un capitulo da
   CERO candidatos, la fila se escribe igual** y dice `SIN SUPERFICIE`.

> ### **ESTOS TRES CAPITULOS SON LOS PASOS `2`, `3` Y `4` DE LA SERIE QUE ABRISTE, Y POR ESO ESTA TAREA TIENE UNA MITAD QUE NO TENIA LA ANTERIOR**
>
> `recorrer_siete_pasos_programa_desarrollo_negocio` (`cap_13`, `L43` a `L57`) es **cabeza de
> `SERIE NUMERADA` con `0` de `7` partes**, y **el auditor te firmo esa lectura entera**: los siete
> titulos son los titulos exactos de siete capitulos completos, verificado uno a uno.
>
> **SI ALGUNO DE ESTOS TRES CAPITULOS PRODUCE SU PROPIA CABEZA, la arista cabeza a parte se declara
> AHI, en tu misma vuelta y citando `cap_13` `L43` a `L57`** (`EXTRACTOR.md` `15.6`; `D.37`). **No
> antes y no despues.** Y si el capitulo da un metodo DENTRO del paso pero no la cabeza del paso
> (que es lo que le paso a `cap_14` con `responder_8_preguntas`), **dilo con esas palabras**: un
> metodo dentro de un paso **no** es la cabeza de ese paso, y cablearlo como si lo fuera fabrica una
> arista falsa.

---

## 3. TAREA `3`: **LA MUESTRA DE FIDELIDAD, SEMILLA `g5`**

    python scripts/muestra_fidelidad.py --libro gerber_emyth --capitulos cap_15,cap_16,cap_17 --semilla g5

**Pega la salida entera.** Quien te audite vuelve a correrlo y tiene que salirle la misma lista:
**una muestra que no se reproduce no es una muestra, es una eleccion.** La semilla reparte cual se
relee ENTERO y cuales van a `15` pasos; **no lo eliges tu.**

**EL DISPARADOR SIGUE EN PIE:** si la muestra de un capitulo pasa del `10` por ciento de pasos
inventados, **ese capitulo se relee entero antes de seguir.**

---

## 4. TAREA `4`: **EL CIERRE, Y UN PASO NUEVO AL FINAL QUE PAGA `d107`**

El cierre de siempre: las guardas con su salida pegada entera (`gate` son **TRES** lineas, no dos,
`d103`), las cifras recomputadas y no copiadas de la apertura, la tabla de cierre, los discutibles
marcados **antes** de saber si aciertas, y las condiciones de parada repasadas una a una con su
medida.

**Y ANTES DE COMMITEAR, UN PASO QUE NO TENIAS: VUELVE A CORRER EL INFORME DEL PRIMER CANDIDATO DE LA
VUELTA.**

> **POR QUE, Y ESTA MEDIDO** (`d107`, `ACTA G4` `1.7` y `3.5`): correr la aduana en el acto hace que
> **el ultimo candidato de una vuelta vea a todos y el primero no vea a ninguno.** En tu vuelta `4`,
> `cambiar_saludo_cliente_dos_ramas` corrio sobre poblacion `452` y publico `1` vecino; al cerrar
> tenia **`3`**. Uno de los dos nuevos quedo leido desde el otro lado; **el otro no lo leyo nadie**, y
> lo tuvo que leer el auditor.
>
> **No es maquinaria nueva: es el mismo instrumento, corrido una vez mas.** Si aparecen vecindades
> que tu informe original no pudo ver, **las lees y las adjudicas ahi mismo**, y si no aparece
> ninguna, **lo dices con la salida pegada**. Con eso la vuelta cierra con su vecindad completa.

---

## 5. LO QUE NO HACES, Y NO ES NEGOCIABLE

- **NO INSERTAS.** `MODO_INSERCION=cuarentena`. Cero nodos al grafo (`D.39`).
- **NO TOCAS EL ARNES NI LA MAQUINARIA.** `D.45`, moratoria total: `orquestador_forja.sh`, `src/`,
  `scripts/`, `tests/`, `hooks/` y `esquema/`. **Mides y subes, no arreglas.**
- **NO TOCAS `d106` AUNQUE TE PIQUE.** El tablero publica hoy `ultimo_capitulo = cap_19` para este
  libro cuando el ultimo minado es `cap_14`, porque `src/tablero.py:133` deriva los capitulos por
  expresion regular sobre el texto entero del candidato y tu cabeza de serie **cita `cap_19` en su
  prosa, que es `D.37` funcionando**. **Ya esta medido, anotado y subido en la `ACTA G4` `6.1`.** No
  es tuyo ni mio: es de `src/`.
- **NO ESCRIBES DOCTRINA.** La cola se queda en `11` (`D.55`). Si encuentras una pregunta nueva,
  **registrala con su medida y dejala ahi.** Si una lectura te pide mover la vara de `9.1`, **eso si
  es parada y se trae.**
- **NO TOCAS `cap_01`, `cap_02` NI `cap_03`.** Siguen en `d094` por decision del fundador, y **no son
  bloqueantes.**
- **NO ABRES UN CUARTO CAPITULO.** Tres es el techo del ligero.

---

## 6. LOS PUNTEROS QUE NO SE PUEDEN PERDER

Estan en `docs/loop/DEUDA.jsonl` y **ninguno se abre en esta vuelta**:

- **`d104`** (`D.37`, `cap_12` `L21`): `Innovation`, `Quantification` y `Orchestration`, ninguna
  existe como nodo cabeza.
- **`d098`** (`D.37`, `cap_05` `L29`): las tres fases del crecimiento. Su gemelo.
- **`d099`**: donde nace el nodo de la delegacion, **si nace**: `cap_18` `L345` a `L349`. `cap_18` no
  esta minado, **y no lo minas en esta vuelta.**
- **`d108`** (nuevo): releer `cap_14` `L27` contra `L117` con las dos delante. **Es para la vuelta que
  INSERTE**, no para esta.

**Y LA CADENCIA, PARA QUE NO TE PILLE:** `python scripts/deuda.py --clase 5` dice `LIBRE`, van `4` de
`5`. **Tu vuelta `6` sale `SANEAMIENTO`** y hay **`38`** deudas esperando.

---

## 7. AL CERRAR

- **Repasa `D.61` contra tu propio reporte antes de cerrarlo**: por cada discutible que publiques, o
  esta **ejecutado** o esta **cerrado con su motivo y la linea del libro delante**. **Ninguno
  abierto**, porque esa figura pesa como `CIFRA PUBLICADA` y su tope es `2`, no `3`.
- **Mide `python forja.py credito`** (solo lectura) y pega la salida. **No uses `--anotar`:** esa
  fila es del auditor (seccion `1`).
- **Mide las condiciones de parada una a una y publica que las mediste.**
- **Commitea `docs/loop/` y tu carpeta de evidencia.**
- **Si nada te obliga a parar, NO escribas `PARA_ALEXIS.md`.**

---

**Cero guiones largos y cero guiones medios. Deja correr el hook. Si algo contradice una regla
vigente, paras y lo traes. No adivines.**
