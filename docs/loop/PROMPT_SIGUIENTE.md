# ENCARGO DE LA VUELTA 53: **REGIMEN LIGERO, LA FRONTERA DE TRES CAPITULOS Y LO QUE QUEPA DENTRO DE SU TECHO**

*Linea **serial** (`extraccion-mundo-11`). Escrito por el auditor en la `ACTA 51`, que cubre la
vuelta `52`. Modo austero (`D.47`): no repito lo que el registro ya dice.*

> # **LIBRO DE ESTA VUELTA: `grove_high_output`**
> # **CLASE DE ESTA VUELTA: EXTRACCION**

**Commitea y pushea lo pendiente en la rama activa antes de tocar nada.**

**El tablero autoriza el libro y lo mido yo antes de escribirlo** (`D.49`, `D.50`):

    $ python forja.py tablero --puedo grove_high_output
    LINEA 'serial', LIBRO 'grove_high_output': SI
      'grove_high_output' esta COSECHADO y sin dueno: su trabajo ya llego a esta rama, asi que
      se continua desde el capitulo siguiente al ultimo minado (cap_05), citando su frontera. D.50.

---

## TAREA 1. **LOS REGISTROS DE LA `ACTA 51`, RECOGIDOS SIN REABRIRLOS**

**Recoge, no rediscutas, y eso incluye lo que te favorece** (`EXTRACTOR.md` `14`).

| # | lo que la `ACTA 51` adjudica | donde vive |
|---:|---|---|
| `1` | **de tus DIECISEIS discutibles se sostienen los DIECISEIS**: `0` caidas dentro del marcado | `51.4` |
| `2` | **firmo tu `0` PUENTE de `35` pasos** y tus seis cifras de palabras, leidos por mi a ciegas antes de ver el reporte | `51.3`, `51.8` |
| `3` | tus `34` `SANO` existen los `34` y los `34` llevan `RAZON`: **`D.8` sin un solo caso**. De `7` releidos por muestra pineada se sostienen `7` | `51.5` |
| `4` | `d051` y `d052` **PAGADAS**, comprobadas por prefijo, por clave y por una regular mas ancha que la tuya | `51.1.a` |
| `5` | **`P15` entra en `d053` con `P12`**: misma figura, y se decide el dia de la insercion. **No lo partas hoy** | `51.4` fila `1` |
| `6` | **`P18` no se parte** | `51.4` fila `8` |
| `7` | la decision del fundador del 19 sep queda leida: **la de saneamiento es la `54`**, y su punto `3` lo corrige su propio cuerpo | `51.2` |
| `8` | **la serie de la forma de los pasos sube a `37,1` por ciento** y queda REGISTRADA y sin adjudicar (`D.56`). **No es caida tuya** | `51.9` |

### 1.a. **TU CAIDA DE `REPORTE`, Y ES LA SEGUNDA SEGUIDA**

> **`REPORTE` sube de `1 de 3` a `2 de 3`.** Tu `NN.5.b` y tu `NN.5.i` titulan que **la prueba de
> aceptacion en rojo es una guarda de DATO**, y la lista de las que bloquean es **cerrada**:
> `gate`, el cerrojo, el censo no decreciente y la fidelidad `D.30`, **y solo esas** (`D.55`).
> **Estaba literal en tu propio encargo.** Vive en CABECERA y en la TABLA de cierre, asi que acumula.

**LO QUE NO SE TE CARGA, Y LO DIGO CON LA MISMA LETRA:** la **conducta** fue la correcta. Declarar,
no arreglar, no escribir `PARA_ALEXIS.md` y cerrar antes las cuatro tareas de produccion es lo que
`EXTRACTOR.md` `7` manda. **Lo que cae es el nombre, y el nombre decide quien repara y cuando.**

**EL REMEDIO VA AQUI Y NO COMO TAREA BLOQUEANTE**, y cito la regla que me lo impide: `D.55` deja como
maximo **una** bloqueante y **solo citando una guarda de DATO en rojo**, y **no hay ninguna de las
cuatro en rojo**. Entre dos reglas fechadas que chocan gana la mas reciente (`D.13`).

> **EL REMEDIO, Y ES DE UNA LINEA:** **antes de publicar cualquier frase que llame `guarda` a algo
> que bloquea, pega la lista cerrada de las cuatro y di a cual de las cuatro pertenece.** Si no
> pertenece a ninguna, **la palabra es `deuda` y se anota con `scripts/deuda.py`.** Se cumple
> escribiendo, no midiendo, y por eso no cuesta turno.

### 1.b. **LAS DOS DEUDAS QUE LA `ACTA 51` ABRE: SE RECOGEN Y NO SE PAGAN HOY**

| id | que es | cuando se paga |
|---|---|---|
| **`d056`** | **tu cola de lectura se movio despues de medirse**: veredictaste `34` pares y sobre el arbol que commiteaste levantan `41`, con `18` sin veredicto y `11` veredictados que ya no levantan. **Causa medida y de metodo, no tuya**: los informes corren de uno en uno y los pagos de `d051` y `d052` engordaron el `resumen_teorico` de las seis hermanas despues de que sus informes hubieran corrido | **el dia de la insercion**, donde `D.58` pone la relectura entera |
| **`d057`** | **la suite en rojo por un fixture que va por detras de su guarda.** No es guarda de DATO, es deuda. **Su arreglo esta en `tests/` o en `scripts/`, que `D.45` te veda** | cuando el fundador levante el veto o arregle el fixture |

**LO QUE `d056` SI TE CAMBIA HOY, Y ES LO UNICO:** cuando escribas el bloque de veredicto de un
candidato, **nombra la poblacion con la que su informe midio**, que es lo que deja reconstruir
despues si la cola se movio. **Ya lo haces**: sigue haciendolo y no anadas nada mas.

---

## TAREA 2. **LA FRONTERA DE `cap_06`, `cap_07` Y `cap_08`, PUBLICADA ANTES DE CORTAR NADA**

**Publica la frontera de cada uno y cierrala contra el cuerpo**: la suma de las filas da el `wc -w`
del cuerpo, **cero lineas sin cubrir y cero solapes**. **Si una no cierra, esa unidad no se mina** y
lo declaras con su cifra.

**LA TABLA VA PEGADA DE SU INSTRUMENTO** (`D.41`), y el tallado la compara celda a celda.

**Y CADA FILA DECLARA CUANTOS NODOS PREVE**, como la `LL.4.b` que esta vuelta acaba de reproducir al
digito: esa columna es la que el auditor siguiente recomputa, asi que **una fila con `0` nodos lleva
su motivo escrito** (definicion, caso del autor, material de cierre).

## TAREA 3. **MINAR, CON EL TECHO POR DELANTE Y CON SUS DOS MITADES**

> # **TECHO: `30` candidatos (`D.58`) O `90` MINUTOS DE RELOJ DE ADUANA, LO QUE LLEGUE ANTES.**

**LA MITAD EN MINUTOS LA ESCRIBO PORQUE `d011` ME OBLIGA, Y ESTA VEZ ES LA QUE MUERDE**, con su
denominador delante y tomada de tu propia medida de `NN.5.f`:

    media por pasada de aduana, vuelta 52 : 731,2 s   (4387,4 s sobre 6 de 6 pasadas medidas)
    30 candidatos a 731,2 s                : 21936 s = 365,6 min = 6,1 horas de aduana sola
    90 min de techo a 731,2 s              : 7,4 candidatos

**ADJUDICO TU PROPUESTA DE `NN.5.f`**, que era tuya de proponer y mia de decidir: **el presupuesto en
minutos se escribe con la cifra de hoy, `731,2` s, y no con los `327,0` de la vuelta 51.** Con eso,
**el techo de `30` de `D.58` no se puede alcanzar en un turno** y el que manda es el de minutos.
**No bajo el techo del fundador: escribo su mitad medida al lado, que es lo que `d011` pide.**

- **Un candidato por vez y en el orden del libro** (`D.36`).
- **Cada uno pasa por `python forja.py informe <candidato>` en el acto de escribirlo**, y su bloque
  de veredicto nombra la poblacion de esa pasada.
- **CERO INSERCIONES.** El lote `7` esta ABIERTO: `D.39` no deja entrar nada, y la puerta la mides tu.
- **Las aristas que la senial no levanta se declaran por lectura** (`D.29`) **con su razon escrita**,
  y **se cablean el dia de la insercion**, no hoy.
- **Si cierras corto, lo declaras con su cifra** (`EXTRACTOR.md` `12.4`): que capitulo quedo abierto,
  en que pieza, y cuantos minutos de aduana consumiste. **Cerrar corto y no decirlo si es caida.**

## TAREA 4. **LA FIDELIDAD, POR MUESTRA Y CON SU SEMILLA**

> # **LA SEMILLA DE ESTA VUELTA ES `v53`, Y SE ESCRIBE EN EL REPORTE.**

    python scripts/muestra_fidelidad.py --libro grove_high_output \
           --capitulos cap_06,cap_07,cap_08 --semilla v53

**El instrumento reparte y no lo eliges tu**, y **su salida se pega entera**: quien te audite vuelve
a correrlo con la misma semilla y **tiene que salirle la misma lista**. Si le sale otra, es caida de
cifra. **Si un capitulo no llega a minarse, su fila lo dice en vez de desaparecer.**

**PUBLICA `PASOS INVENTADOS` POR CAPITULO**, una fila por capitulo y no una media, **diciendo cual es
de muestra y cual entero**.

> ### **EL DISPARADOR, Y ES OBLIGATORIO**
>
> **Si la muestra de un capitulo pasa del `10` por ciento de pasos inventados, ESE CAPITULO SE RELEE
> ENTERO ANTES DE SEGUIR.** Sobre `15` pasos, el tope de `D.30` no se afloja: lo que cambia es que
> aqui escala a relectura entera en vez de a veredicto.

## TAREA 5. **EL CIERRE**

    python forja.py gate
    python forja.py guiones
    python scripts/cerrar_reporte.py
    python scripts/tabla_de_cierre.py --escribir      y se pega su salida
    python forja.py tablero --escribir

**UN ACTA CORTA** (`D.58`): la frontera al digito, el cotejo de la muestra con su semilla,
`PASOS INVENTADOS` por capitulo, el estado recomputado al cierre y **la linea del tramo con su
reloj**, sea cual sea el numero, incluido `0`. **Lo que el registro ya dice no se repite** (`D.47`).

**Y EL CIERRE VA A SALIR EN ROJO POR `d057`, Y NO ES TUYO:** `cerrar_reporte.py` nombrara
`prueba de aceptacion`. **Lo declaras citando `d057` y sigues.** **No lo llames guarda de DATO**
(`TAREA 1.a`) y **no lo arregles**, que `D.45` te veda `tests/` y `scripts/`.

---

## LO QUE NO ES TAREA TUYA

| | |
|---|---|
| **la deuda** | `19` pendientes. **No la pagues hoy.** **La vuelta `54` ES de saneamiento** y el arnes lo comprueba al abrir: no depende de que nadie se acuerde |
| **las nueve de maquinaria** | `d020`, `d022`, `d028`, `d029`, `d030`, `d033`, `d037`, `d047` y `d057` estan vedadas por `D.45`. **Se citan y no se tocan** |
| **`P12` y `P15`** | son `d053`. **No los partas**, ni siquiera si el corte te parece evidente |
| **la doctrina** | congelada en `11`. Pregunta nueva: **registrala con su medida y dejala ahi** |
| **los `6` de `d005`** | son de `cap_03` y se reparan **antes de insertar**, no antes de minar |

> **LO UNICO QUE TE BLOQUEA ES UNA GUARDA DE DATO EN ROJO, Y SON CUATRO Y SOLO CUATRO** (`D.55`):
> **`gate`, el cerrojo, el censo no decreciente y la fidelidad `D.30` con puente.** Eso no es deuda:
> **es averia.** Todo lo demas se anota con `scripts/deuda.py` y la vuelta sigue.

## LO QUE NO SE TOCA

- **`src/`, `scripts/`, `tests/`, `hooks/`, el banco, el arnes y los protocolos** (`D.45`).
- **El banco no gana reglas nuevas** salvo que una guarda de DATO lo exija con su cita.
- **`config/frentes.json` y `config/umbrales.json`** se leen, no se editan.
- **El bucle no funde ramas y el bucle no crea remotos.**

---

**Cero guiones largos y cero guiones medios. Deja correr el hook. Si algo contradice una regla
vigente, paras y lo traes. No adivines.**
