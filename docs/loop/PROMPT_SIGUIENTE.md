# ENCARGO DE LA VUELTA 9 DEL FRENTE `gerber_emyth`: **LA ULTIMA. `cap_01`, `cap_02` Y `cap_03`, Y EL LIBRO QUEDA ENTERO**

*Linea **`gerber_emyth`** (`extraccion-gerber_emyth`, worktree
`C:/Users/AlexDesk/Documents/forja-gerber_emyth`). **Escrito por la sesion de chat del 22
sep 2026** al aplicar el punto `1` de la decision del fundador `GERBER ENTRA, MARQUET
SIGUE`.*

> # **LIBRO DE ESTA VUELTA: `gerber_emyth`**
> # **CLASE DE ESTA VUELTA: EXTRACCION**

**Commitea y pushea lo pendiente en la rama activa antes de tocar nada.**

---

## 0. LA NOTICIA, PORQUE CAMBIA COMO LEES ESTA VUELTA

**`gerber_emyth` ENTRA AL MUNDO `11` COMO SEXTO LIBRO.** El fundador lo decidio hoy, y la
clausula que lo permite es de `D.60`: *un sexto libro entra solo si cierra su extraccion
antes de que la cuota de la semana se agote*. **La cerraste ayer, y por eso existe esta
decision.**

**QUE SIGNIFICA PARA TI, EN UNA LINEA: esta es tu ULTIMA vuelta.** Al cerrarla, el fundador
funde tu rama a `extraccion-mundo-11` y **tus `22` candidatos, mas los que den estos tres
capitulos, pasan a esperar la insercion de la semana que viene.**

> **NO TE DA PRISA Y NO TE PIDE MAS VOLUMEN.** Te dice que **lo que dejes escrito hoy es lo
> que el libro lleva dentro para siempre**, porque despues de esta vuelta este frente se
> desmonta. Un capitulo firmado a la ligera hoy **no tiene vuelta `10` que lo repare.**

---

## 1. LA TAREA: **LOS TRES QUE `d094` APARTO**

El `21` sep el fundador mando empezar el frente en `cap_05`, y `cap_01`, `cap_02` y
`cap_03` quedaron apartados **con su cita**, en `d094`. **Hoy se levanta ese aparte.**

| capitulo | palabras |
|---|---:|
| `cap_01` | `1434` |
| `cap_02` | `1244` |
| `cap_03` | `2237` |
| **total** | **`4915`** |

**SON TRES Y ES EL TECHO DEL LIGERO** (`D.58`). No abras un cuarto: **no queda ninguno.**

### 1.a. **LO QUE SEA ENSAYO SE FIRMA EN CERO, CON SU MOTIVO**

**La decision lo dice con esas palabras, y no es una concesion: es la instruccion.** Este
libro abre con material de tesis, y **es muy posible que los tres den cero.** Ya lo hiciste
bien cinco veces en este mismo frente (`cap_05`, `cap_06`, `cap_16`, `cap_20`, `cap_21`,
`cap_22`), y la casa entera lo tiene escrito:

> **Un capitulo vacio no se puede comprobar por muestra: no hay pasos que muestrear.** La
> unica verificacion posible es **leerlo ENTERO y decir contra que se leyo.**

**Tu `ACTA G8` `3.2` es el modelo de como se firma un cero**, con el tramo que compite
nombrado y la razon de por que no pasa la vara. **Hazlo igual.**

**LO QUE NO VALE es el silencio.** `PASOS INVENTADOS POR CAPITULO` lleva **una fila por
capitulo aunque la fila diga `SIN SUPERFICIE`** (`AUDITOR_FORJA.md` `8`): un cero es una
medida, no una ausencia.

### 1.b. **Lo que entregas por cada uno de los tres**

1. **LA FRONTERA, fila a fila contra el fichero**, con sus numeros de linea y sus palabras,
   **cero solapes y cero lineas sin cubrir**. Se comprueba al digito.
2. **Los candidatos que de**, en `cuarentena/gerber_emyth/<id>.json`, **cada uno con su
   `python forja.py informe` corrido en el mismo acto en que lo escribes**, y **cada uno
   con su `UNIDAD DE ORIGEN: fuentes/gerber_emyth/cap_NN.md` en el `resumen_teorico`**.
3. **`PASOS INVENTADOS POR CAPITULO`**, una fila por capitulo.
4. **La muestra de fidelidad con la semilla de esta vuelta:**

       python scripts/muestra_fidelidad.py --libro gerber_emyth --capitulos cap_01,cap_02,cap_03 --semilla g9

> ### **EL PUNTO `2` NO ES BUROCRACIA Y AYER COSTO CARO**
>
> El `21` sep se arreglo un defecto del tablero que llevaba semanas: leia `cap_NN` en
> **cualquier parte** del fichero, asi que un candidato tuyo que escribia *como paso con
> `cap_09` y `cap_10`* **marcaba esos dos como minados cuando decia justo lo contrario.**
> Ahora el capitulo sale de la `UNIDAD DE ORIGEN` que la ficha declara. **La ficha que no
> la declare se lee con la regla vieja**, o sea mal. **Declarala.**

---

## 2. TAREA `2`: **CERRAR `d094` Y DEJAR LA FRONTERA DEL LIBRO COMPLETA**

**La decision pide las dos cosas, y la segunda es la que vale dentro de un anio:**

1. **`d094` se cierra** con un `pago` en `docs/loop/DEUDA.jsonl`, citando esta vuelta y los
   tres capitulos leidos.
2. **La frontera del libro queda completa de `cap_01` a `cap_22`.** Publica la tabla del
   libro entero, una fila por unidad, con **su estado y su firma**: minado con candidatos,
   o minado en cero con el acta que lo firma.

**ESA TABLA ES LO QUE SE LLEVA LA COSECHA.** Cuando este frente se desmonte, su reporte y
su acta se archivan; **la tabla del libro es lo que la vuelta de insercion va a leer para
saber que tiene delante.**

---

## 3. LO QUE YA ESTA HECHO Y NO HAY QUE REHACER

- **`19` de `22` unidades minadas**, `cap_04` a `cap_22`, sin hueco.
- **`22` candidatos en bandeja**, todos pasados por la aduana en seco.
- **Tus nueve capitulos en cero estan firmados y declarados** en
  `config/frentes.json`, clave `minados_en_cero`, con sus cuatro actas. **Ya no hace falta
  que los pidas: la `ACTA G8` `4.4` los pidio y estan escritos.**
- **El tablero ya te da `19` de `22` y `ultimo_capitulo` `cap_22`**, que es la verdad.

---

## 4. LOS PUNTEROS QUE LA VUELTA DE INSERCION NO PUEDE PERDER

**No se abren hoy, pero esta es la ultima vuelta que puede repasarlos**, asi que
**compruebalos y dilo**:

- **`d098`** (`D.37`, `cap_05` `L29`) y **`d104`** (`D.37`, `cap_12` `L21`): las dos ternas
  sin cabeza. **Si alguno de los tres capitulos de hoy hace nacer una de esas cabezas, la
  arista se declara AHI y no despues.**
- **`d108`**: releer `cap_14` `L27` contra `L117`.
- **`d111`**: la serie de `cap_13`, medida en `0` de `7`, con su primera arista declarable
  ya identificada (`REPORTE.md` `G8.5.c`).

---

## 5. LO QUE NO HACES

- **NO INSERTAS.** `MODO_INSERCION=cuarentena`. Ningun frente inserta nunca.
- **NO TOCAS EL ARNES NI LA MAQUINARIA.** `D.45`, moratoria total. **Mides y subes.**
- **NO ESCRIBES DOCTRINA.** Si hace falta regla nueva, es parada.
- **NO FUNDES TU RAMA NI LA COSECHAS.** El paso `(b)` de `D.50` es del fundador, y lo hara
  el cuando esta vuelta cierre. **El bucle no funde ramas y el bucle no crea remotos.**
- **NO ABRES `gerber_emyth_cap17_reservado`.** Su condicion escrita sigue sin cumplirse:
  entra cuando su propio libro ya este en el grafo, y hoy hay `0` nodos suyos.

---

## 6. AL CERRAR

- **Escribe tu tanda**: `python forja.py credito --anotar`. Las cinco especies estan en `0`
  y esta es tu ultima oportunidad de dejarlas asi.
- **Repasa `D.61` contra tu propio reporte**: cada discutible que publiques, **ejecutado o
  cerrado con su motivo. Ninguno abierto.** Y hoy pesa el doble, porque **no hay vuelta
  siguiente que recoja lo que dejes abierto.**
- **Commitea `docs/loop/` y tu carpeta de evidencia.**
- **Escribe `PARA_ALEXIS.md` con el estado de cierre del libro**, que es lo que el fundador
  necesita delante para fundir: censo por capitulo, candidatos en bandeja, guardas y las
  cinco rachas. **Y deja `PROMPT_SIGUIENTE.md` VACIO**, porque despues de esta vuelta este
  frente no abre otra.
